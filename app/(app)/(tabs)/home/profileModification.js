import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const profileModification = () => {
    // page pour modifier les informations de l'utilisateur
    // formulaire avec input text : username, description, image de profil, adresse
    // bouton pour envoyer les modifications
    // bouton pour annuler les modifications et revenir sur le profil


  return (
    <View>
      <Text>profileModification</Text>
    </View>
  )
}

export default profileModification

const styles = StyleSheet.create({})